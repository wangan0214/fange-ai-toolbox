---
title: 帆哥PPT编辑器 · 竞品研究与改进路线
version: v1.0.0
timestamp: 2026-08-06
source_trust: |
  小红书原帖（ClickDeck + Lens）为「外部核实源·一手帖」；NextPPT / HtmlDrag / HeyHTML /
  GrapesJS 等为「外部核实源·公开横评/论坛」。均非用户指定源，按铁律标注来源分层。
---

# 帆哥PPT编辑器 · 竞品研究与改进路线（v1.0.0 · 2026-08-06）

> 起源：用户发来小红书帖 `xhslink.cn/o/AaMrT8z1Xhh`（主打「html 微调 / 轻松修改 html / UI 设计 / PPT 制作」），
> 要求研究并抽象需求、找出可改进点。本文件即研究成果 + 落地优先级。

## 一、这个工具到底是什么（来源澄清）

小红书帖主推 **ClickDeck** —— 一个**浏览器插件**，解决「AI 生成 HTML 之后最后一公里微调贵」的痛点。
同帖顺带介绍 **Lens**（HTML 深度阅读/笔记工具，另一条产品线，与本编辑器关系不大）。

- **ClickDeck V4.0 核心能力**（外部核实源·小红书公开帖 + 评论区印证）：
  - 不写代码，在真实页面上**可视化改 HTML**，降低和编程 AI 的沟通成本
  - 支持直接编辑 **SVG 矢量图、Canvas 画布**等复杂元素
  - 新增**意见批注**：框选区域写意见、标记删除、拖拽调整位置
  - 重构拖拽意图识别，让 AI 更精准理解修改需求
  - 导出 PDF / 长截图 / 生成**编程 AI 能读懂的英文 Prompt**继续迭代
  - 评论区印证：Edge 可用；AI 用法是「点击即复制，粘贴给侧边栏 AI」
- **最像我们的竞品 NextPPT**（外部核实源·论坛帖）：本地 HTML PPT 编辑器，拖入 AI 生成的 html → 点哪改哪 →
  导出 PPTX/PDF/PNG；技术用 iframe 沙箱 + postMessage/patch 协议，File System Access API 读写，
  Puppeteer 高 DPI 截图，但 PPTX 是**图片型**（不可编辑元素）
- **同类横评**（外部核实源·CSDN）：HeyHTML（即开即编、数据本地化）、HtmlDrag（Figma 式自由画布）、GrapesJS 等 8 款

## 二、需求抽象（7 层模型）

把这类工具抽象成 7 层需求模型，**核心 Pain 只有一个**：AI 写 HTML 已经很强，但「最后 1 公里」——
挪 8 像素、换一句话、改个颜色——回对话框 re-prompt 会丢上下文、烧 token、常改坏其他页。
工具的使命就是把这条「生成→微调→re-prompt→重生成」的昂贵链路，砍断成「打开→点改→导出」。

| 层 | 需求 | 说明 |
|---|---|---|
| **L0 接入** | 打开任意 HTML、本地优先、零上传 | 工作起点 |
| **L1 可视化精修** | 改字 + 拖拽/缩放/吸附 + SVG/Canvas 编辑 | 「真编辑器」的核心 |
| **L2 代码兜底** | 可视化改不动时切代码 patch | 兜底逃生舱 |
| **L3 安全可追溯** | 自动保存 / 历史快照 / 回滚 / 隔离 | 改错能救 |
| **L4 交付** | 导出 PPTX / PDF / PNG / 长截图 | 给不会看 HTML 的人 |
| **L5 AI 闭环** | 批注 / 生成给编程 AI 的 prompt | 降沟通成本（ClickDeck 亮点） |
| **L6 知识沉淀** | 模板库 / 风格复用 | 非本工具目标 |

## 三、与我们编辑器的对照（fange-html-deck-editor v1.0.22）

| 层 | 我们现状 | 评价 |
|---|---|---|
| L0 接入 | open-in-place 打开任意位置 HTML、本地优先 | **已有·强** |
| L1 可视化 | 双击改字✓；**拖拽/缩放/吸附/SVG 编辑✗** | **最大缺口** |
| L2 代码兜底 | 无集成代码视图切换 | 缺口 |
| L3 安全可追溯 | 自动保存/历史快照/回滚/隔离（历史存编辑器自家目录） | **已有·且比竞品强** |
| L4 交付 | PPTX（editable+image）✓；**PDF/PNG/长截图✗** | 部分 |
| L5 AI 闭环 | 无批注、无「生成 AI prompt」 | 缺口 |
| L6 知识沉淀 | 非目标 | — |

## 四、可改进的地方（按优先级）

**P0 — 决定「像不像专业编辑器」的核心一跃**
1. **拖拽 + 缩放 + 对齐吸附**：把当前纯 contenteditable 改字，升级为「选中元素→拖拽移动/缩放 handle + 对齐辅助线」。
   这是从「只能改字」到「真编辑器」的最大差距，NextPPT / ClickDeck 都有。
2. **代码视图兜底**：集成轻量代码视图（Monaco 或 textarea+高亮），可视化改不动时切代码 patch，改完回可视。

**P1 — 交付完整度 & 差异化**
3. **导出 PDF / PNG / 长截图（高 DPI）**：PPTX 已有，补齐其余交付格式。
4. **AI 协作闭环**：一键「生成给编程 AI 的英文 prompt」（含当前 DOM 片段 + 修改意图），或复制上下文给侧边栏 AI。
   这是 ClickDeck 的差异化亮点，且契合「降沟通成本」理念。
5. **批注/标注模式**：框选区域写意见、标记删除/拖拽调整——适合把稿子丢给别人反馈，或自记修改意图。

**P2 — 高阶**
6. **SVG / Canvas 复杂元素编辑**（ClickDeck V4 重点，成本高，视需求）。
7. **更细的 in-session 多步撤销栈可视化**（当前靠历史快照，缺会话内逐步 undo）。

## 五、我们的既有优势（改进时绝不能丢）

- **原地编辑不污染源**：历史/渲染/日志全在编辑器自家目录（`sha1(路径)` 命名空间），绝不往用户项目塞 `.history`——
  比 NextPPT（绑 File System Access API、限 Chrome/Edge）和 ClickDeck（浏览器插件）更稳、跨浏览器。
- **可编辑 PPTX**：我们有 `build_editable_pptx`（元素可编辑），NextPPT 只是图片型 PPTX——真优势，保留。
- **100% 本地、免注册、无上传**：隐私强项。
- **deck 结构感知**：slide 导航 + 视口比例检测，是通用编辑器（HtmlDrag/HeyHTML）没有的。

## 六、落地建议（待用户拍板）

1. 先把本文件作为 `17-帆哥PPT编辑器/` 的规划基线（已带时间戳+版本）。
2. 从 **P0-1 拖拽缩放+对齐吸附** 开始落地（编辑器已有 v1.0.18 的画布拖拽+吸附基础，可在此基础上接入缩放 handle）。
3. 交付格式（P1-3）与 AI 闭环（P1-4）作为第二批。
